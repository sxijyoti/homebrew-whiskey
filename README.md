# Homebrew Tap for Whiskey

Install Whiskey with Homebrew:

```bash
brew tap sxijyoti/whiskey
brew install whiskey
```

The formula in this repository is automatically updated whenever a new Whiskey release is published.